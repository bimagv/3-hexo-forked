<div class="title-list">
    <form onkeydown="if(event.keyCode === 13){return false;}">
        <input id="local-search-input" class="search" type="text" placeholder="<% if (theme.searchAll){%>Full-text search:<%} else {%>Search...<%}%>" />
        <i class="cross"></i>
        <span style="margin-right: 7em;">
            <label for="fuckinwitch">Post:</label>
            <input id="fuckinswitch" type="checkbox" style="display: none" />
            <i id="fuckinWitchIcon"></i>
        </span>
        <span>
            <label for="tagswitch">Tags:</label>
            <input id="tagswitch" type="checkbox" style="display: none" />
            <i id="tagsWitchIcon"></i>
        </span>
    </form>
    <%- partial('_partial/fuckin') %>
    <%- partial('_partial/tag') %>
    <% if (theme.searchAll) { %>
    <div id="local-search-result">

    </div>
    <% } %>
    <nav id="title-list-nav">
        <% site.posts.forEach(function(post, i){  %>
        <a <% if(post.top){%>id="top"<%}%> class="<% post.categories.forEach(function(category, i){ %><%=category.name%> <% }) %>"
           href="<%- url_for(post.path) %>"
           data-tag="<% post.tags.forEach(function(tag, i){ %><%=tag.name%><% if (i+1<post.tags.length){%>,<%}})%>"
           data-author="<% if(theme.author && theme.author.on==true && post.author) {%><%=post.author %><%}%>" >
            <span class="post-title" title="<%=post.title %>"><%=post.title %></span>
            <span class="post-date" title="<%= date(post.date, 'YYYY-MM-DD HH:mm:ss')%>"><%= date(post.date, 'YYYY/MM/DD') %></span>
        </a>
        <% }) %>
    </nav>
</div>